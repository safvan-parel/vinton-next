export default function Dashboard() {
    return (
        <div className="admin-card">
            <h1>Dashboard</h1>
            <p>Welcome to the admin panel. Use the sidebar to manage sliders, blogs, and settings.</p>
        </div>
    );
}
